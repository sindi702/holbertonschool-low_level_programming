#include "main.h"
/**
 *_isalpha - main func
 * Return: Always 0 (Success)
 * @c: The character to print
 */
int _isalpha(int c)
{
	if ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'))
	{
		return (1);
	}
	else
	{
		return (0);

		c++;
}
return (0);
}
