#include "main.h"
/**
 *  * print_alphabet - main func
 *   * Return: Always 0 (Success)
 *    */
int _islower(int c)
{
	if(c >= 'a' && c <= 'z')
{
	return (1);
}
else
{
	return (0);
	c++;
}
return (0);
}
