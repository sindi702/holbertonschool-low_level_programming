#ifndef _FUNCTION_LIKE_MACRO
#define _FUNCTION_LIKE_MACRO
/**
 * ABS(X) - computes the absolute value of a number
 * @x: number that his value needs to be computed
 */

#define ABS(x) (((x) < 0) ? -(x) : (x))
#endif
