#include "main.h"
/**
 * _islower - main func
 * Return: Always 0 (Success)
 * @c: The character to print
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
{
	return (1);
}
else
{
	return (0);
	c++;
}
return (0);
}
